import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  imageContainer: { height: 200, marginBottom: 10, width: 200 },
  textContainer: { alignItems: 'center', flexDirection: 'row', gap: 5 },
})
